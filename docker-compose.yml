services:
  divide:
    build: .
    restart: unless-stopped
    ports:
      - "3050:3050"
    volumes:
      - divide-data:/data  # SQLite-Datenbank überlebt Container-Updates
    env_file:
      - .env

volumes:
  divide-data:
