services:
  divide:
    image: ghcr.io/haenrick/divide:latest
    restart: unless-stopped
    ports:
      - "${PORT:-3050}:${PORT:-3050}"
    volumes:
      - divide-data:/data
    env_file:
      - .env

volumes:
  divide-data:
